--- a/newlib/configure
+++ b/newlib/configure
@@ -2556,7 +2556,7 @@
    *) as_fn_error $? "bad value ${enableval} for newlib-retargetable-locking" "$LINENO" 5 ;;
  esac
 else
-  newlib_retargetable_locking=no
+  newlib_retargetable_locking=yes
 fi
 
  if test x$newlib_retargetable_locking = xyes; then
